(() => {
    const row = document.getElementsByClassName('row-con');

    // contenedor principal, número de graficas, cantidad de filas, cantidad de columnas
    render_main_chart(row[0],2,10,8);
})()